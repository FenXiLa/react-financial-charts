### 总体思路

**“无损升级”本质上是：每一步升级后，功能、API、打包产物都通过验证，可以随时回滚。**  
你现在这份 `必须升级的包分析.md` 已经把“要升什么”说清楚了，下面要做的是“怎么升”和“怎么验”。

---

### 1. 升级前的基础保护

- **开分支**
  - 从 `main` 拉一个长期分支，例如：`chore/deps-security-upgrade`.
- **锁文件备份**
  - `cp package-lock.json package-lock.before-security-upgrade.json`
- **确认当前基线是绿色**
  - **根目录**执行：
    ```bash
    npm install
    npm run build
    npm run lint
    # 如果项目有 test/storybook：
    npm test
    npm start  # 确认 Storybook 能正常跑起来
    ```
  - 只有当前是“全绿”的，后面才好判断是哪个升级引入的问题。

---

### 2. 小步、分批升级，而不是一口气全升

结合你文档里的优先级，建议拆成几批，每一批**改动越少越好**，每批都完整跑一次验证：

1. **批次 A：安全性最高 & 变更相对可控**
   - `npm audit fix`（自动能修的先修）
   - 手动升级：
     ```bash
     npm update @babel/core @babel/helpers @babel/runtime @babel/traverse --save-dev
     npm update axios
     npm update body-parser
     npm update brace-expansion
     ```
   - 然后立刻验证：
     ```bash
     npm install
     npm run build
     npm run lint
     npm test      # 有的话
     npm start     # Storybook 能正常打开、示例能正常渲染
     ```
   - 如果全部 OK，**立刻 commit 一次**，例如：`chore: security deps batch A`.

2. **批次 B：Lerna 升级（高风险，单独一批）**
   - 在当前分支再开一个子分支，例如：`chore/lerna-8-upgrade`.
   - 按文档方案执行（例如）：
     ```bash
     npm install lerna@latest --save-dev
     ```
   - 对比 `lerna.json` 和官方迁移指南，按要求调整配置/命令。
   - 验证时不仅要跑 root 的脚本，还要跑 Lerna：
     ```bash
     npx lerna --version
     npx lerna bootstrap
     npx lerna run build
     npx lerna run lint
     ```
   - 如果构建、lint、Storybook、示例全部正常，再合并这个分支。

3. **批次 C：Storybook 6 → 8（非常大的变更，独立项目级别的工作）**
   - 单独新分支 `feat/storybook-8`.
   - 严格按照官方迁移指南，一步步升级（通常会建议 6→7→8 分步）。
   - 这里已经超出“安全补丁”的范畴，更像“功能性/技术栈升级”，可以慢慢搞，不一定和安全升级绑在一起。

---

### 3. 每一批次如何“验证无损”

每做完**一小批依赖升级**，执行同一套验证流程，目的是保证“代码行为没变或只在我们预期内变”。

- **自动检查**
  - **安装和构建**
    ```bash
    npm install
    npm run build
    ```
  - **静态检查**
    ```bash
    npm run lint
    ```
  - **单元/集成测试（如果有）**
    ```bash
    npm test
    ```
- **手动验证（对这个项目比较关键）**
  - 启动 Storybook：
    ```bash
    npm start
    ```
  - 粗略点开几类 Story（比如：K 线、Indicator、Interactive 工具）：
    - 图表能渲染出来（无白屏 / 控制台致命报错）
    - 鼠标移动、缩放、拖动等交互如旧
- **产物对比（可选但更严格）**
  - 在升级前后各打一个 build，把 `dist` 或对应打包产物做一次差异比较：
    - 粗略看：文件数/文件名是否差异巨大
    - 严格一点：用 `diff`/`git diff` 看是否多出奇怪的大改动
  - 对开源库来说，只要对外导出的 API 未变（TypeScript 类型无大范围变化），就基本可以认为是“无损”。

---

### 4. 出问题时如何“无痛回滚”

**保证“可回滚”是“无损升级”的底线。**

- **每一批调整都单独 commit**
  - 例如：
    - `chore: security deps batch A (babel/axios/body-parser)`
    - `chore: bump lerna to 8.x`
- **一旦跑验证发现问题**
  - 直接：
    ```bash
    git reset --hard HEAD~1
    cp package-lock.before-security-upgrade.json package-lock.json  # 如有必要
    npm install
    ```
  - 回到上一批“已确认安全”的状态。
- **对比锁文件**
  - 升级前后，用：
    ```bash
    git diff package-lock.json
    ```
  - 确认只有你**理解的那几条依赖链**发生了变化，没有“顺带升了一大堆乱七八糟的东西”。

---

### 5. 具体到你这份升级计划的小建议

- **安全相关的那几个（@babel/*、axios、body-parser、brace-expansion）**  
  完全可以按你文档的“方案 A”做一批，只要跑齐上面那套验证流程，风险可控。
- **Lerna 升级 & Storybook 升级**  
  不要和安全补丁绑在一起做，**独立分支、独立验证、允许较长测试周期**，否则一出问题很难分辨是安全补丁还是工具升级导致的。

---

### 6. 如果你想让我“带命令走一遍”

如果你愿意，我可以按照上面的“分批方案”，帮你设计一套**完全对应你仓库的具体命令序列和 commit 切分**（包括每一步该跑哪些脚本、看到什么算通过），你只需要在本地按顺序执行即可。




### 总览：你要做的几批次 & 每批的目标

下面所有命令都默认在项目根目录：`/Users/luosen/Documents/GitHub/react-financial-charts` 下执行。

- **批次 0**：确认当前基线是“全绿”  
- **批次 A**：安全相关依赖（`@babel/*` / `axios` / `body-parser` / `brace-expansion`）  
- **批次 B**：Lerna 6.6.2 → 8.2.4（或 latest 8.x）  
- **批次 C**：Storybook 6.1.20 → 8.x（单独大任务）  

每一批都有：**命令顺序 → 预期修改文件 → 跑哪些脚本 → 通过标准 → commit 切分建议**。

---

### 批次 0：基线检查（在 `main` 上）

#### 命令顺序

```bash
cd /Users/luosen/Documents/GitHub/react-financial-charts

# 确认干净
git status

# 可选：拉最新 main
git pull

# 备份锁文件
cp package-lock.json package-lock.before-security-upgrade.json

# 安装依赖 + 基线验证
npm install
npm run build
npm run lint
npm test      # 如果有测试实现
npm start     # 手动打开 Storybook 检查几页
# 检查完 Ctrl+C 结束
```

#### 通过标准

- `npm run build`、`npm run lint`、`npm test` **退出码为 0**。
- 启动 `npm start` 后，Storybook 可以正常打开，至少：
  - K 线、指标、交互工具等几个代表性故事页能正常渲染；
  - 控制台没有红色报错。
- `git status` 显示 **工作区干净**（或仅有 `package-lock.before-security-upgrade.json` 这类你预期的文件）。

> 如果这里不“全绿”，先修好再往下，否则后面很难判断是哪一步升级引入的问题。

---

### 批次 A：安全依赖升级（Babel / axios / body-parser / brace-expansion）

在 `main` 上拉一个分支专门做安全升级：

```bash
cd /Users/luosen/Documents/GitHub/react-financial-charts
git checkout -b chore/deps-security-upgrade
```

#### 步骤 A1：自动 `npm audit fix`（可选但推荐单独一 commit）

```bash
npm audit fix
```

- **预期修改文件**
  - `package-lock.json`（通常不改 `package.json`）
- **验证脚本**

```bash
npm install
npm run build
npm run lint
npm test      # 如有
npm start     # 手动点几页 Storybook
# 检查完 Ctrl+C 结束
```

- **通过标准**
  - 所有命令退出码为 0；
  - Storybook 正常渲染，无新报错。
- **commit 建议**

```bash
git status   # 确认只有锁文件等你理解的变更
git diff     # 粗看下变更范围

git add package-lock.json
git commit -m "chore: npm audit fix (auto security updates)"
```

#### 步骤 A2：手动升级 Babel 相关依赖

```bash
npm update @babel/core @babel/helpers @babel/runtime @babel/traverse --save-dev
```

- **预期修改文件**
  - `package.json`（`devDependencies` 中 `@babel/*` 版本变化）
  - `package-lock.json`
- **验证脚本**

```bash
npm install
npm run build
npm run lint
npm test      # 如有
npm start     # 打开 Storybook，随便切几个故事页
# 检查完 Ctrl+C
```

- **通过标准**
  - `build/lint/test` 全部 0 退出；
  - Storybook 中 TS/打包错误没有新增；
  - 没有因为 Babel 变化导致的运行期异常（比如某些组件不再渲染）。
- **commit 建议**

```bash
git status
git diff

git add package.json package-lock.json
git commit -m "chore: bump babel deps for security fixes"
```

#### 步骤 A3：手动升级 axios / body-parser / brace-expansion

```bash
npm update axios
npm update body-parser
npm update brace-expansion
```

> 它们大概率只在 `package-lock.json` 里出现（间接依赖），`package.json` 通常不会直接有这几个。

- **预期修改文件**
  - 主要是 `package-lock.json`
- **验证脚本**

```bash
npm install
npm run build
npm run lint
npm test
npm start
# 检查完 Ctrl+C
```

- **通过标准**
  - 所有命令退出码 0；
  - Storybook 功能没退化。
- **commit 建议**

```bash
git status
git diff

git add package-lock.json
git commit -m "chore: bump axios/body-parser/brace-expansion for security"
```

#### 批次 A 收尾

- 再跑一次全套验证（确保整个分支最终状态是“全绿”）：

```bash
npm install
npm run build
npm run lint
npm test
npm start
# 检查完 Ctrl+C
```

- 合并到 `main`（建议开 PR 审一下）：

```bash
git checkout main
git merge --no-ff chore/deps-security-upgrade -m "chore: merge security deps upgrades"
```

---

### 批次 B：Lerna 6.6.2 → 8.2.4（高风险，单独分支）

在已经合并完批次 A 的最新版 `main` 上继续：

```bash
cd /Users/luosen/Documents/GitHub/react-financial-charts
git checkout -b chore/lerna-8-upgrade
```

#### 步骤 B1：升级 Lerna 版本

（可以按你文档写死到 8.2.4，避免 “latest” 带来额外不确定性）

```bash
npm install lerna@8.2.4 --save-dev
```

- **预期修改文件**
  - `package.json`（`devDependencies.lerna`）
  - `package-lock.json`
- **quick sanity check**

```bash
npx lerna --version           # 应该输出 8.x.x
npx lerna ls                  # 能列出 packages，即为基本可用
```

#### 步骤 B2：Lerna 相关脚本验证

你根目录 `scripts` 中大量依赖 Lerna，按实际脚本一一验证：

```bash
# 1) bootstrap & link
npm run bootstrap             # lerna bootstrap
npm run link || true          # 如果脚本本身有问题，这步可能要后续修复

# 2) 核心构建流程
npm run build                 # lerna run build
npm run lint
npm test                      # 如有

# 3) Storybook / docs
npm run docs                  # run-s build docs:stories
npm start                     # 手动查看 Storybook
# 检查完 Ctrl+C

# 4) 其他 lerna 命令 sanity check（不一定每次都跑，但建议至少确认能跑通）
npx lerna run build --scope='*/core'
npx lerna run build --scope='*/series'
```

- **通过标准**
  - 上述所有命令能正常执行（对于 `link` 这种历史脚本，如果本来就有问题，可以在 commit message 里说明，不要模糊不清）；
  - Storybook 一样能正常看 demo；
  - `npx lerna ls`、`npx lerna run build` 无重大报错。
- **commit 建议**

```bash
git status
git diff

git add package.json package-lock.json
git commit -m "chore: bump lerna to 8.2.4"
```

> 如果在这个过程中你不得不调整 `package.json` 里的某些 `scripts`（比如 Lerna v8 某些子命令有细微变化），**建议单独一个 commit**：

```bash
git add package.json
git commit -m "chore: adjust npm scripts for lerna 8"
```

#### 批次 B 收尾

- 再跑一遍关键脚本：

```bash
npm run build
npm run lint
npm test
npm start
# 检查完 Ctrl+C
```

- 合并回 `main`：

```bash
git checkout main
git merge --no-ff chore/lerna-8-upgrade -m "chore: upgrade lerna to 8.x"
```

---

### 批次 C：Storybook 6 → 8（大升级，独立规划）

这个本身就是一个“项目级任务”，可以按下面方式开一个**长期功能分支**：

```bash
cd /Users/luosen/Documents/GitHub/react-financial-charts
git checkout -b feat/storybook-8
```

#### 建议的 commit 粒度

1. **commit 1：6.x → 7.x 兼容改动 + 基础跑通**
   - 命令顺序（示例）：
     ```bash
     npm install @storybook/react@7 --save-dev
     # 按官方迁移指南调整 main.js / preview.js 等
     npm run docs
     npm start
     ```
   - commit：`feat: migrate storybook to v7`

2. **commit 2：7.x → 8.x 改动**
   - 继续按官方迁移指南修 main 配置、CSF、DocBlocks 等。
   - commit：`feat: migrate storybook to v8`

3. **中间如果需要批量调整 stories 代码**（API 变更等），可以再拆 1～2 个 commit，不要把所有 Storybook 改动堆在一个 commit 里。

#### 每次改动的通过标准

- `npm run docs` 成功；
- `npm start` 可以正常启动，新版本 Storybook UI 可以打开，核心 demo 页面无致命错误。

---

### 出问题时如何回滚（配合上面 commit 切分）

在任何一个批次中，如果你发现本批次的变更引入了严重问题：

```bash
# 回滚到上一个 commit
git reset --hard HEAD~1

# 如有必要，把锁文件恢复到备份
cp package-lock.before-security-upgrade.json package-lock.json
npm install
```

然后你可以重新根据上面的“命令序列 + 通过标准”再尝试一遍。

---

如果你愿意，我也可以按你未来实际跑命令时的终端输出，帮你一条一条定位是哪个升级引入了问题，并给出相应的回滚或修复建议。